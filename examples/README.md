# examples/

Schema-validated example artifacts. Examples exist to demonstrate the canonical
models and to exercise the test harness — they are **not** implementations.

- `adaptive-trading-platform/project.manifest.yaml` — the first project ASEP is
  intended to generate, expressed purely as a `ProjectManifest`. It contains no
  trading logic, no strategy, and no secrets.
