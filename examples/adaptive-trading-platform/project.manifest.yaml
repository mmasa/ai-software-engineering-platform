# Example ProjectManifest — the Adaptive Trading Platform.
#
# This is the first project ASEP is intended to generate. It is included here
# ONLY as a schema-validated example of the canonical Project Manifest.
# It contains no trading logic, no strategy, and no secrets.
#
# Validates against: core/schemas/project-manifest.schema.json
apiVersion: asep.dev/v1alpha1
kind: ProjectManifest
id: PRJ-ADAPTIVE-TRADING
name: Adaptive Trading Platform
description: >-
  Reference project generated by ASEP. Used to exercise the platform's
  generation, governance, and localization pipelines.
canonicalLanguage: en
locales:
  - en
  - ja
  - vi
  - ko
profiles:
  - PRF-ISO27001-BASELINE
  - PRF-SECURE-BACKEND
repository:
  topology: monorepo
  defaultBranch: main
  primaryLanguage: python
controls:
  - CTL-AC-0001
requirements:
  - REQ-0001
risks:
  - RSK-0001
metadata:
  note: Illustrative example only. Not an implementation.
